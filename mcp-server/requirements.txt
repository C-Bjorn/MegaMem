# MCP Server Requirements
# MCP SDK (Model Context Protocol)
mcp>=1.0.0

# Install ALL provider extras for maximum compatibility (matches original install.py behavior)
# This allows users to switch between providers without reinstalling dependencies
# Upgraded to 0.22.x for MinHash dedupe, multilingual support, and unicode improvements
graphiti-core[falkordb,anthropic,google-genai,azure-openai,groq,voyage,sentence-transformers]~=0.22.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Additional dependencies
neo4j>=5.0.0
psutil>=5.9.0

# WebSocket support
websockets>=12.0
aiohttp>=3.9.0
aiohttp-cors>=0.7.0
