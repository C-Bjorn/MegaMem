# MCP Server Requirements
# The key dependency is graphiti-core[mcp] which includes the original MCP server
graphiti-core[mcp]>=0.20.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Additional dependencies that might be needed
neo4j>=5.0.0

# WebSocket support
websockets>=12.0
aiohttp>=3.9.0
aiohttp-cors>=0.7.0
